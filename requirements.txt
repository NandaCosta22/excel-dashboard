anyio==4.9.0
argon2-cffi==25.1.0
argon2-cffi-bindings==21.2.0
arrow==1.3.0
asttokens==3.0.0
async-lru==2.0.5
attrs==25.3.0
babel==2.17.0
beautifulsoup4==4.13.4
bleach==6.2.0
certifi==2025.7.9
cffi==1.17.1
charset-normalizer==3.4.2
colorama==0.4.6
comm==0.2.2
contourpy==1.3.2
## Minimal runtime requirements for excel-dashboard
## Trimmed to the packages the app actually needs to run the web app and generate charts.
## For Python 3.13: keep core packages and install heavy/optional tooling separately.

# Web framework
Flask>=2.3.3,<3

# Data processing & Excel
pandas>=2.3.1
openpyxl>=3.1.0  # engine used by pandas.read_excel for .xlsx

# Plotting
plotly>=6.2.0

# Optional convenience for environment variables (not required)
python-dotenv>=1.0.0

# Notes:
# - Heavy packages (torch, jupyter, etc.) are intentionally omitted and listed in
#   `requirements-optional.txt` below. Install them only when needed.
# - After upgrading to Python 3.13, run a fresh virtual environment and `pip install -r requirements.txt`.
jedi==0.19.2
